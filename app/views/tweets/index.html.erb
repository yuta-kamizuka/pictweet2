<div class="contents row">
  <% @tweets.each do |tweet| %>
    <div class="content_post" style="background-image: url(<%= tweet.image %>);">
      <div class="more">
      <%# image_tagとはHTMLの<img>タグを生成するためのメソッドになります。imageディレクイトリ配下の画像を表示させる %>
        <span><%= image_tag 'arrow_top.png' %></span>
        <ul class="more_list">
          <li>
          <%# edit_tweet_path tweetsコントローラーのeditアクションへのPrefixは、edit_tweetです。つまり指定すべきパスはedit_tweet_pathである  rails routesで見れる %>
            <%=link_to '編集', edit_tweet_path(tweet.id), method: :get %>
            <%= link_to '削除', tweet_path(tweet.id), method: :delete %>
          </li>
        </ul>
      </div>
      <p><%= tweet.text %></p>
      <span class="name">
        <%= tweet.name %>
      </span>
    </div>
  <% end %>
</div>